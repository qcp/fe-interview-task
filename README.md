# Homework: Expense Tracker

Тестовое задание для кандидата на позицию **Frontend Developer (Intern)**.

Среднее время выполнения тестового задания: от одного до четырёх часов. 

---

## Описание

Создай приложение **"Трекер расходов"** (Expense Tracker) — инструмент для учёта личных трат.

---

## Структура страницы

Приложение состоит из трёх блоков (порядок на твоё усмотрение):

1. **Форма добавления** — ввод новой траты
2. **Блок итогов** — общая сумма и суммы по категориям
3. **Список расходов** — все добавленные траты с возможностью удаления

Дизайн свободный — главное, чтобы было аккуратно и понятно.

---

## Функциональные требования

### Компонент 1: Форма добавления траты

#### Поля формы

| Поле | HTML-элемент | Обязательное | Placeholder/Label |
|------|--------------|--------------|-------------------|
| Сумма | `<input type="number">` | Да | "Введите сумму" |
| Категория | `<select>` | Да | "Выберите категорию" |
| Описание | `<input type="text">` | Да | "На что потратили?" |
| Дата | `<input type="date">` | Да | — |

#### Значения для выпадающего списка категорий

```javascript
const CATEGORIES = [
  { id: 'food', name: 'Еда' },
  { id: 'transport', name: 'Транспорт' },
  { id: 'entertainment', name: 'Развлечения' },
  { id: 'shopping', name: 'Покупки' },
  { id: 'other', name: 'Прочее' }
];
```

#### Правила валидации

| Поле | Правило | Сообщение об ошибке |
|------|---------|---------------------|
| Сумма | Число > 0 | "Сумма должна быть больше 0" |
| Сумма | Не пустое | "Введите сумму" |
| Категория | Выбрана из списка | "Выберите категорию" |
| Описание | Минимум 3 символа | "Минимум 3 символа" |
| Описание | Не пустое | "Введите описание" |
| Дата | Не пустая | "Выберите дату" |
| Дата | Не больше сегодняшней | "Дата не может быть в будущем" |

#### Поведение формы

| Действие | Ожидаемый результат |
|----------|---------------------|
| Форма невалидна | Кнопка "Добавить" неактивна |
| Клик на невалидное поле и blur | Показать ошибку под полем |
| Клик "Добавить" (форма валидна) | Трата добавляется в список, форма очищается |
| После добавления | Фокус возвращается на поле "Сумма" |

---

### Компонент 2: Список расходов

#### Колонки/поля

| Поле | Формат отображения | Пример |
|------|-------------------|--------|
| Дата | `DD.MM.YYYY` | 22.12.2024 |
| Категория | Название из CATEGORIES | Еда |
| Описание | Как введено | Обед в кафе |
| Сумма | `X XXX ₽` (разделитель тысяч + символ рубля) | 1 650 ₽ |
| Удаление | Кнопка/иконка | 🗑️ или × |

#### Поведение списка

| Состояние/Действие | Ожидаемый результат |
|--------------------|---------------------|
| Список пустой | Показать текст "Нет расходов" |
| Есть расходы | Отсортированы по дате (новые сверху) |
| Клик на кнопку удаления | Трата удаляется из списка |
| После удаления | Итоги пересчитываются автоматически |

---

### Компонент 3: Блок итогов

#### Что отображать

1. **Общая сумма** всех расходов
2. **Сумма по каждой категории** (только те, где есть траты)

#### Пример отображения

```
Всего потрачено: 15 430 ₽

По категориям:
• Еда: 5 200 ₽
• Транспорт: 3 100 ₽
• Развлечения: 7 130 ₽
```

#### Правила

| Правило | Описание |
|---------|----------|
| Форматирование сумм | С разделителем тысяч: `1 234 ₽` |
| Пустые категории | Не показывать (если 0 трат) |
| Пересчёт | Автоматически при добавлении/удалении |
| Если нет расходов | Показать "Всего потрачено: 0 ₽" |

---

### Компонент 4: Сохранение в localStorage

#### Требования

| Требование | Описание |
|------------|----------|
| Ключ | `expenses` |
| Формат | JSON-массив объектов |
| Когда сохранять | После каждого добавления/удаления |
| Когда загружать | При первом рендере приложения |

#### Структура данных (псевдокод)

```typescript
// Один расход
Expense {
  id: string          // уникальный ID
  amount: number      // сумма в рублях
  category: string    // id категории
  description: string // описание траты
  date: string        // дата в формате ISO
  createdAt: string   // время создания записи
}
```

#### Пример данных

```json
[
  {
    "id": "a1b2c3d4",
    "amount": 650,
    "category": "food",
    "description": "Обед в кафе",
    "date": "2024-12-22",
    "createdAt": "2024-12-22T12:30:00.000Z"
  },
  {
    "id": "e5f6g7h8",
    "amount": 100,
    "category": "transport",
    "description": "Метро",
    "date": "2024-12-21",
    "createdAt": "2024-12-21T09:15:00.000Z"
  }
]
```

---

## Технические требования

| Требование | Обязательно | Детали |
|------------|-------------|--------|
| Фреймворк | Да | **Vue**, React, Svelte - на выбор |
| Язык | Да | JavaScript или TypeScript |
| Семантичная вёрстка | Да | Использовать `<form>`, `<button>`, `<input>`, `<label>`, `<table>`/`<ul>` |
| CSS | Да | Любой подход: CSS Modules, Tailwind, styled-components, обычный CSS |
| Git | Да | Осмысленные коммиты |
| README | Да | Инструкция по запуску |

---

## Усложнения (опционально)

Усложнения - не обязательны, это возможность продемонстрировать дополнительные навыки и знания.

| Бонус | Что добавить | Подсказка |
|-------|--------------|-----------|
| +1 | **TypeScript** | Типизировать Expense, категории, пропсы компонентов |
| +1 | **Фильтрация по категории** | Dropdown над списком для выбора категории |
| +1 | **Фильтрация по датам** | Два date-input: "от" и "до" |
| +1 | **Редактирование траты** | Клик на трату → открывается форма с данными → сохранение |
| +1 | **Мета-фреймворк** | **Nuxt**, Next.js, SvelteKit, any else |
| +2 | **Диаграмма расходов** | Pie chart или bar chart по категориям (можно использовать библиотеку) |
| +2 | **Тестирование** | Unit тесты

## Формат сдачи

Ссылка на **GitHub-репозиторий**.

Если репозиторий приватный, необходимо выдать доступ `OneEyed1366` и `qcp`.

### README должен содержать:

1. **Как запустить проект** — команды для установки и запуска
2. **Какой фреймворк использован** — и почему выбран именно он
3. **Какие усложнения реализованы** — список из таблицы выше
4. **Что бы добавил** — если бы было больше времени (опционально)

---

## Пример структуры репозитория

```
expense-tracker/
├── src/
│   ├── components/
│   │   ├── ExpenseForm.tsx      # Форма добавления
│   │   ├── ExpenseList.tsx      # Список расходов
│   │   ├── ExpenseItem.tsx      # Один элемент списка
│   │   └── Summary.tsx          # Блок итогов
│   ├── hooks/
│   │   └── useLocalStorage.ts   # (опционально) хук для localStorage
│   ├── types/
│   │   └── expense.ts           # (опционально) типы TypeScript
│   ├── constants/
│   │   └── categories.ts        # Константы категорий
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── README.md
└── .gitignore
```

---

## Что НЕ нужно делать

- Авторизация / бэкенд
- Сложные анимации

---

## Чек-лист перед сдачей

Проверь себя:

- [ ] Приложение запускается без ошибок
- [ ] Можно добавить расход (все поля валидируются)
- [ ] Расходы отображаются в списке
- [ ] Можно удалить расход
- [ ] Итоги пересчитываются автоматически
- [ ] Данные сохраняются после перезагрузки страницы
- [ ] README содержит инструкцию по запуску
- [ ] Есть несколько осмысленных коммитов
